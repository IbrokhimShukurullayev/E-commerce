import React, { memo } from 'react'
import "./login.scss"

const Login = () => {

    return (
        <>
            <section id="login">
                <h2>Login</h2>
            </section>
        </>
    )
}

export default memo(Login)